// *VibrationButton.js* ``` jsx import React, { useState } from 'react'; const VibrationButton = () => { const [vibrating, setVibrating] = useState(false); const handleVibrate = () => { // Check if vibration is supported if ('vibrate' in navigator) { // Vibrate for 500ms navigator.vibrate(500); setVibrating(true); setTimeout(() => setVibrating(false), 500); } else { console.log('Vibration not supported'); } }; return ( <button onClick={handleVibrate} style={{ backgroundColor: vibrating ? 'gray' : 'primary', color: 'white', padding: '10px 20px', borderRadius: '5px', cursor: 'pointer', }} > {vibrating ? 'Vibrating...' : 'Vibrate'} </button> ); }; export default VibrationButton; ``` *Usage* ``` jsx import React from 'react'; import VibrationButton from './VibrationButton'; const App = () => { return ( <div> <VibrationButton /> </div> ); }; `